global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'echo-server-monitor'

scrape_configs:
  - job_name: 'echo-server'
    static_configs:
      - targets: ['echo-server:8080']
    metrics_path: '/metrics'
    scrape_interval: 10s
